package com.company;

public class EmailValidation {
    public static void main(String[] args) {
        String email = "john@gmail.com";
        Boolean isValid = false;

        // check if the email is valid based on these criterias
        // it has @ sign
            // it has a domain .com, .id
    }
}
